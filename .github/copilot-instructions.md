# GitHub Copilot Instructions

Before proposing or editing code, follow the repository-wide instructions in `AGENTS.md`, then read `OVERVIEW.md`, `RULES.md`, `DECISIONS.md`, and `AI_WORKFLOW.md`.

Do not duplicate or diverge from those rules here. AI session logs belong only in `.ai-work/`, which must remain ignored by Git.
